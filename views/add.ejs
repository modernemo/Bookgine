<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><% if (review) { %>Edit<% } else { %>Create<% } %> Review</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

    <div class="container py-5">
        <div class="card shadow-sm">
            <div class="card-header bg-dark text-white">
                <h3><% if (review) { %>Edit<% } else { %>Create<% } %> Review</h3>
            </div>
            <div class="card-body">
                <form action="/reviews<% if (review) { %>/<%= review.id %>/edit<% } %>" method="POST">

                    <div class="mb-3">
                        <label for="book" class="form-label">Book Title</label>
                        <textarea class="form-control" id="body" name="body" rows="4" required><%= review ? review.body : '' %></textarea>
                    </div>

                    <div class="mb-3">
                        <label for="rating" class="form-label">Rating (1–5)</label>
                        <input type="number" class="form-control" id="rating" name="rating" min="1" max="5" required
                            value="<%= review ? review.rating : '' %>">
                    </div>

                    <button type="submit" class="btn btn-success">Submit</button>
                    <a href="/reviews" class="btn btn-secondary ms-2">Cancel</a>

                </form>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
