<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Review Details</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

    <div class="container py-5">
        <div class="card shadow-sm">
            <div class="card-header bg-dark text-white">
                <h3 class="mb-0"><%= review.body %></h3>
            </div>
            <div class="card-body">
                <h5 class="card-title">Rating: <span class="badge bg-success"><%= review.rating %>/5</span></h5>
            </div>
            <div class="card-footer d-flex justify-content-between">
                <a href="/reviews/<%= review.id %>/edit" class="btn btn-warning">Edit</a>
                <a href="/reviews/<%= review.id %>/delete" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this review?')">Delete</a>
            </div>
        </div>

        <div class="mt-3">
            <a href="/reviews" class="btn btn-secondary">← Back to Reviews</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>